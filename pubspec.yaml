name: inventory
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.3.2
  json_annotation: ^4.12.0
  path_provider: ^2.1.5
  shared_preferences: ^2.1.0
  image_picker: ^1.1.2
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3 # Use the latest version

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/boxicon.png"

flutter:
  uses-material-design: true
